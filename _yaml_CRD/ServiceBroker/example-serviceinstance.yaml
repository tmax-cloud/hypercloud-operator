apiVersion: servicecatalog.k8s.io/v1beta1
kind: ServiceInstance
metadata:
  name: nginx-instance
  namespace: hypercloud-system
spec:
  serviceClassName: nginx-template
  servicePlanName: example-plan1
  parameters:
    NAME: nginx
    IMAGE: nginx:1
